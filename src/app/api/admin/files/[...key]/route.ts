import { requireUser } from "@/lib/auth";import{getStoredFile}from"@/lib/storage";
export async function GET(_:Request,{params}:{params:Promise<{key:string[]}>}){await requireUser(["SUPER_ADMIN","ADMIN"]);const key=(await params).key.map(decodeURIComponent).join("/");if(!key.startsWith("private/"))return new Response("Not found",{status:404});try{const file=await getStoredFile(key);return new Response(Buffer.from(file.bytes),{headers:{"content-type":file.contentType||"application/octet-stream","content-disposition":'attachment; filename="document"',"cache-control":"private, no-store"}});}catch{return new Response("Not found",{status:404});}}
