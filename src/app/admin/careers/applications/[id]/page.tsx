import{notFound,redirect}from"next/navigation";import{prisma}from"@/lib/prisma";import{deleteApplicationAction,updateApplicationAction}from"@/app/actions/applications";
export default async function Page({params}:{params:Promise<{id:string}>}){const id=(await params).id;const a=await prisma.jobApplication.findUnique({where:{id},include:{career:{select:{title:true}}}});if(!a)notFound();return <div className="admin-dashboard"><section className="admin-page-intro"><div><p>{a.status}</p><h2>{a.applicantName}</h2><p>{a.career.title} · {a.email}</p></div><a className="admin-primary-button" href={a.cvUrl} download>Download CV</a></section><section className="admin-form-card"><p>{a.coverLetter}</p>{a.portfolioUrl&&<a href={a.portfolioUrl} target="_blank" rel="noreferrer">Open portfolio</a>}</section><form action={updateApplicationAction} className="admin-form-card"><input type="hidden" name="id" value={id}/><select name="status" defaultValue={a.status}>{["NEW","REVIEWING","SHORTLISTED","INTERVIEW","REJECTED","HIRED"].map(x=><option key={x}>{x}</option>)}</select><textarea name="internalNotes" rows={8} defaultValue={a.internalNotes??""}/><button className="admin-primary-button">Save</button></form><form action={async f=>{"use server";await deleteApplicationAction(f);redirect("/admin/careers/applications");}}><input type="hidden" name="id" value={id}/><button className="admin-secondary-button">Delete Application</button></form></div>;}
