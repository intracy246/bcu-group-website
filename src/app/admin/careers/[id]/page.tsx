import Link from"next/link";import{notFound}from"next/navigation";import{getAdminCareer}from"@/lib/career-service";
export default async function Page({params}:{params:Promise<{id:string}>}){const c=await getAdminCareer((await params).id);if(!c)notFound();return <div className="admin-dashboard"><section className="admin-page-intro"><div><p>{c.status}</p><h2>{c.title}</h2><p>{c.summary}</p></div><Link className="admin-primary-button" href={`/admin/careers/${c.id}/edit`}>Edit Career</Link></section><section className="admin-form-card"><p>{c.company} · {c.department} · {c.location}</p>{c.description.map((p,i)=><p key={i}>{p}</p>)}<small>Created {new Date(c.createdAt).toLocaleString()} · Updated {new Date(c.updatedAt).toLocaleString()}</small></section></div>;}
