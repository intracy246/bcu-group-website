import Link from "next/link";import {notFound} from "next/navigation";import {listAdminProjects} from "@/lib/project-service";
export default async function Page({params}:{params:Promise<{id:string}>}){const id=(await params).id;const project=(await listAdminProjects()).find(item=>item.id===id);if(!project)notFound();return <div className="admin-dashboard"><section className="admin-page-intro"><div><p>{project.status}</p><h2>{project.name}</h2><p>{project.summary}</p></div><Link className="admin-primary-button" href={`/admin/projects/${id}/edit`}>Edit Project</Link></section><section className="admin-form-card"><p>{project.company} · {project.category}</p><p>{project.description}</p><p>{project.tags.join(", ")}</p><small>Updated {new Date(project.updatedAt).toLocaleString()}</small></section></div>;}
