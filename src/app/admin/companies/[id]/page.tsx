import Link from"next/link";import{notFound}from"next/navigation";import{getAdminCompany}from"@/lib/company-service";
export default async function Page({params}:{params:Promise<{id:string}>}){const c=await getAdminCompany((await params).id);if(!c)notFound();return <div className="admin-dashboard"><section className="admin-page-intro"><div><p>{c.status}</p><h2>{c.name}</h2><p>{c.summary}</p></div><Link className="admin-primary-button" href={`/admin/companies/${c.id}/edit`}>Edit Company</Link></section><section className="admin-form-card"><h3>{c.shortName}</h3><p>{c.description}</p><p>{c.industry} · {c.location}</p><p>{c.related.projects} projects · {c.related.news} news · {c.related.careers} careers</p><small>Created {new Date(c.createdAt).toLocaleString()} · Updated {new Date(c.updatedAt).toLocaleString()}</small></section></div>;}
