import{requireUser}from"@/lib/auth";import{prisma}from"@/lib/prisma";import{createUserAction,deleteUserAction,resetPasswordAction,updateUserAction}from"@/app/actions/users";
export default async function Page({searchParams}:{searchParams:Promise<{q?:string;role?:string}>}){await requireUser(["SUPER_ADMIN"]);const s=await searchParams;const roles=["SUPER_ADMIN","ADMIN","EDITOR"];const users=await prisma.user.findMany({where:{...(roles.includes(s.role??"")?{role:s.role as"ADMIN"}:{}),...(s.q?{OR:[{name:{contains:s.q,mode:"insensitive"}},{email:{contains:s.q,mode:"insensitive"}}]}:{})},select:{id:true,name:true,email:true,role:true,status:true,lastLoginAt:true,createdAt:true},orderBy:{createdAt:"desc"}});return <div className="admin-dashboard"><section className="admin-page-intro"><div><p>Security</p><h2>Administrators</h2><p>Manage roles, access and credentials.</p></div></section><form action={createUserAction} className="admin-form-card"><h3>Add administrator</h3><div className="admin-form-grid"><input name="name" placeholder="Name" required/><input name="email" type="email" placeholder="Email" required/><select name="role"><option>EDITOR</option><option>ADMIN</option><option>SUPER_ADMIN</option></select><input name="password" type="password" placeholder="Secure password" minLength={12} required/></div><button className="admin-primary-button">Add User</button></form><section className="admin-data-panel">{users.map(user=><article className="admin-form-card" key={user.id}><form action={updateUserAction} className="admin-form-grid"><input type="hidden" name="id" value={user.id}/><input name="name" defaultValue={user.name}/><input name="email" type="email" defaultValue={user.email}/><select name="role" defaultValue={user.role}>{roles.map(role=><option key={role}>{role}</option>)}</select><select name="status" defaultValue={user.status}><option>ACTIVE</option><option>DISABLED</option></select><button>Save</button></form><p>Last login: {user.lastLoginAt?.toLocaleString()??"Never"} · Created {user.createdAt.toLocaleDateString()}</p><form action={resetPasswordAction}><input type="hidden" name="id" value={user.id}/><input name="password" type="password" minLength={12} placeholder="New password" required/><button>Reset password</button></form><form action={deleteUserAction}><input type="hidden" name="id" value={user.id}/><button>Delete</button></form></article>)}</section></div>;}
