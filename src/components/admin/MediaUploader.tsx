"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";
export default function MediaUploader(){const router=useRouter();const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const response=await fetch("/api/admin/media",{method:"POST",body:new FormData(e.currentTarget)});const result=await response.json();setMessage(response.ok?"Upload complete.":result.error||"Upload failed.");setBusy(false);if(response.ok){e.currentTarget.reset();router.refresh();}}return <form className="admin-form-card" onSubmit={submit}><div className="admin-form-grid"><label className="admin-form-field"><span>File</span><input name="file" type="file" required/></label><label className="admin-form-field"><span>Folder</span><input name="folder" defaultValue="general" required/></label><label className="admin-form-field"><span>Alternative text</span><input name="altText"/></label></div>{message&&<p role="status">{message}</p>}<button disabled={busy} className="admin-primary-button">{busy?"Uploading…":"Upload Media"}</button></form>;}
