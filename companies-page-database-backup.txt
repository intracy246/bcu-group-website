import Link from"next/link";import{listPublicCompanies}from"@/lib/company-service";
export const dynamic="force-dynamic";
export default async function Page(){const companies=await listPublicCompanies();return <><section className="companies-hero"><div className="section-container"><p className="section-kicker">BCU Group Portfolio</p><h1>Companies built for <span>long-term African impact.</span></h1><p>Explore the active companies operating across the BCU Group portfolio.</p></div></section><section className="section"><div className="section-container"><div className="companies-grid">{companies.map(c=><article className="company-card" key={c.id}><div><span>{c.industry||"BCU Group Company"}</span><h2>{c.name}</h2><p>{c.summary}</p></div><Link href={`/companies/${c.slug}`}>Explore {c.shortName} <span>↗</span></Link></article>)}</div>{!companies.length&&<div className="news-empty-state"><span>No companies</span><h2>Company profiles will appear here when activated.</h2></div>}</div></section></>;}
